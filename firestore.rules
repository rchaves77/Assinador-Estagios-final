rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Highly permissive rules for development and preview of all collections and subcollections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
